__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.log
.DS_Store
Thumbs.db

# Local scan reports and scratch datasets produced while manually running
# the CLI -- never part of the repo.
report.json
*.report.json
/tmp-dataset/
/test-dataset/
*.local

# Benchmark scratch output, if ever generated inside the repo by mistake
# (benchmarks/README.md documents generating it outside the repo instead).
/benchmarks/*-dataset/
/benchmarks/*.result.json
result-*.json
